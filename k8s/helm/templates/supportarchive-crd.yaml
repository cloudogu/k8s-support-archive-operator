apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: supportarchives.k8s.cloudogu.com.k8s.cloudogu.com
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  labels:
  {{- include "helm.labels" . | nindent 4 }}
spec:
  group: k8s.cloudogu.com.k8s.cloudogu.com
  names:
    kind: SupportArchive
    listKind: SupportArchiveList
    plural: supportarchives
    singular: supportarchive
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: SupportArchive is the Schema for the supportarchives API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SupportArchiveSpec defines the desired state of SupportArchive.
            properties:
              foo:
                description: Foo is an example field of SupportArchive. Edit supportarchive_types.go
                  to remove/update
                type: string
            type: object
          status:
            description: SupportArchiveStatus defines the observed state of SupportArchive.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
